apiVersion: v2
name: daemon
description: DaemonSet fixture — initContainers, sidecars, tlsSelfSigned, node scheduling
type: application
version: 0.1.0
appVersion: "0.1.0"
dependencies:
  - name: platform
    version: ">=2.0.0-0"
    repository: file://../../../platform-library
    import-values:
      - defaults
