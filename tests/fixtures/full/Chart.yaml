apiVersion: v2
name: full
description: Full-coverage fixture exercising platform-library generators
type: application
version: 0.1.0
appVersion: "0.1.0"
dependencies:
  - name: platform
    version: ">=2.0.0-0"
    repository: file://../../../platform-library
    import-values:
      - defaults
