apiVersion: v2
name: aurora-gateway
description: Helm chart for the Aurora Gateway service used for platform harness testing
type: application
version: 0.5.2
appVersion: "0.5.2"

dependencies:
  - name: platform
    version: 1.0.0
    repository: file://../platform-library
    import-values:
      - defaults
