{{/* HPA applies only to Deployment and StatefulSet; DaemonSets are node-scaled. */}}
{{- define "platform.hpa" -}}
{{- if or (eq .Values.workload.type "Deployment") (eq .Values.workload.type "StatefulSet") }}
{{ include "platform.autoscaling" . }}
{{- end }}
{{- end }}

